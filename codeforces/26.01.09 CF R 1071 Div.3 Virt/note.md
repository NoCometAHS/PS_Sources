# 리뷰 안 했음

# A
척 보는 데 안 풀리더라 ㅋㅋ;;
계속 생각하다가 뭔가 눈에 보이길레 증명이고 뭐고 바로 써서 풀었어.

---
비번은 길이 n의 문자열
밑의 조건을 만족.
- k개 이하의 알파벳만 사용한다.
- (j - i)가 x로 나누어 지는 모든 1 <= i <= j <= n을 만족하는 인덱스 쌍에 대해, s_i != s_j

조건을 만족 못하는 문자열 길이 n의 최솟값을 구하라.

i를 고정시기키 생각해 봤을 때, i + x, i + 2x ... 들은 모두 달라야 한다. 
처음 x개는 같아도 상관이 없네? x개마다 문자가 무조건 바뀌어야 하고.

k개의 문자열만 쓸 수 있으니깐 x*k + 1 이 정답이네.

**인덱스 2개일때 하나 고정하고 보는 방법이 통찰을 주네.**

---
해설 :
x로 나눈 나머지가 같은 부분수열들은 x개 최대 길이는 k개니깐 답이 x*k + 1이라고 하네

# B
브루트 포스 문제

---
해설:
ㅇㅇ 같음.

# C
조건을 찾고, 문제 해결을 잘 해내갔다.
굳굳

---
연산을 여러번 적용할 필요가 있는가?
-> 지금은 잘 모르겠네. 뭔값으로 만들 줄 알고 그래.
*이거 생각 했어야 했는데, 풀때는 문제 잘 못 읽고 연산 1개가 모든 원소에 적용하는 거라고 잘 못 봤었네 ㅋㅋ;*

근데 한번만 하면 될 듯. 나머지가 x이고 싶으면 num - x로 나누면 되는 거 아니냐. 아닌데;;
뭔가 풀이가 말이 안 되는 듯 되는 듯. 10에서 나머지 5로 만들고 싶어. 5로 나눠? 그렴 0인데, 
문제에서 그런 경우가 나올 수 있는 거 아니냐? ㅇㅇ 불가능하고.


어떻게 같게 만들 수 있을까
- 0으로 만들기
    k가 배열의 최솟값이면 전부 0으로 만들 수 있어.

    더 커지게는 못 할까?
        일단 2번째 최솟값 이상이면 안 됨. 어떤 수로 만들려고 하던 간에 ㅇㅇ.
        못 바꾸는 수가 2개가 되고, 모든 원소는 다르니깐 무조건 동일하게 만들 수 없어.
    
        최솟값 ~ 2번째 최솟값-1 범위 안에 있겠네.
            만약 k가 최솟값보다 커진다면, 모든 나머지는 최솟값이 되야겠네.

            그렇다면 나머지가 최솟값이 되게 하는 공통의 약수?를 찾아야 겠네
            어떻게 찾지?
                최솟값 제외 하고, 모든 원소에 최솟값을 뺀 배열 b를 생각하자.
                ~~이 모든 수의 최대 공약수 찾으면 그게 k겠다.~~
                ~~만약 최대 공약수가 배열의 최솟값보다 작으면, 배열의 최솟값이 k고.~~
                약수 중 가장 큰데, 배열 b에서는 최솟값을 골라야 한다.
                어떤 수 n에서 가장 큰 약수는 자기 자신. 그러니깐 최솟값 찾는게 맞네. ㅋㅋ

b에서 최솟값을 선택한게 답이 되는 경우는

b_i/2 > min 의 경우야.
ㅇㅇㅇ
그럴 때는 모든 **b_i/2 미만 나머지는 모두 나누는 수를 바꿔가며 만들 수 있다**고 하네.
짝수여서 b_i/2가 안 되는 경우여도 그럴 때는 b_i/2 == min 일때니깐 상관 없을 듯.

---
해설 : 
ㅇㅇ 나랑 같음. 정렬해서 풀었다는 것만 빼고




# D
빠르게 문제 이해해서 구현 했는데,
특정한 케이스 발견 못하고 시간 많이 써버렸네;;

---
일단 S(p)가 최대려면?
    0이 한번 나오면 그 자리는 계속 0으로 나와.
    최대한 0이 되는 자리를 느리게 만들어야지
    1111
    0111
    0011
    1011
    이런 느낌으로
사전순으로 빠를려면?
    0이 나오는 부분을 왼쪽 비트에서부터 차례대로.
    0인 자리에 다시 1 넣는 건 오른쪽부터.

---
해설 :
ㅇㅇ 나랑 같음.

# E
전에 푼 문제랑 느낌이 비슷한데, 잘 모르겠다.

---
하한 값으로 다 넣어두고.

투표에서 지는 곳들의 투표 합을 따로 저장해놔.

그럼 x, y에 비해 얼마나 많고 적은지 알 수 있어.

지는 곳들의 표들 옮겨서 할 수 없으면 no고 있으면 yes 아니냐.


---
해설: 
힌트 1:
이기는 정당의 투표수 하한은 floor(p_i/2)+1


위 힌트와 한 정당이 모두 이기는 경우를 가지고 열심히 케이스 워크 하면 되네.

# F
문제 뭐임 이거ㅋㅋ.
2번 실행하네.
r -> g -> b -> r
로 순서를 정했을 때
해당 색이 있는 곳으로만 가면 되나?

---
해설
**이분 그래프에서 인접한 정점끼리는 한 정점으로부터 거리가 1 차이 난다.**

내가 생각했던 것처럼 거리에따라 색 배분 해주고 이동하면 되네 ㄷㄷ.

# H
f(x) = x * (x & -x)

1 4 3 16 5 32 12 ...

---
해설
계수를 이모스 법으로 저장하는 방법을 쓰는 것 같은데?

나중에 다시 한 번 보자.