#include "bits/stdc++.h"

using namespace std;
using LL = long long;
using iipair = pair<int,int>;
using graphT = vector<vector<int>>;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int tc;
    cin >> tc;
    
    while (tc--) {
        int n, m;
        cin >> n >> m;
        
    }
    return 0;
}

/*
현재 값 이상의 값으로 움직이는 경로를 그리드라고 한다.
방문한 곳의 값의 합이 경로의 값.

*/