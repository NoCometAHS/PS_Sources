#include "bits/stdc++.h"

using namespace std;
using LL = long long;
using iipair = pair<int,int>;
using graphT = vector<vector<int>>;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    return 0;
}

/*
소문자로 구성된 문자열 S
빈문자열 T
2명이 번갈아 가면서 아무 소문자를 T의 뒤에 붙임.
근데 붙인 결과가 S 부분 문자열이 아니면 못 함.

처음으로 못 움직이는 놈이 패배.

이기는 놈은 누구?

*/